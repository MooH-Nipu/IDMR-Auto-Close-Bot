# LAN IP host. Keep exact; avoid 0.0.0.0 unless intentionally exposing every interface.
BIND_IP=192.168.31.194
PORT=5000
HOST_UID=1000
HOST_GID=1000

# Comma-separated exact HTTPS IDMR origins.
IDMR_ALLOWED_ORIGINS=https://idmr.example.internal
